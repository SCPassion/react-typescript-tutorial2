// Centralized all the reusable types in one file

export type Color = "red" | "blue" | "green" | "purple" // limited to these colors with union type

export type ButtonColor = "red" | "blue" | "green" | "purple" // limited to these colors with union type
